import { NotZeroDirective } from './not-zero.directive';

xdescribe('NotZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NotZeroDirective();
    expect(directive).toBeTruthy();
  });
});
